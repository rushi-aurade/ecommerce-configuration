spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ecomm_dev_db
    username: root
    password: Rushikesh
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  boot:
    admin:
      client:
        url: http://localhost:1111/

  app:
    images:
      upload-dir: "C:/Users/Admin/Product-Images"

server:
  port: 7071

management:
  endpoints:
    web:
      exposure:
        include: "*"
